import type { TeamMember } from "@/lib/types";

/**
 * The "Meet The Team" section on /about stays hidden while this list is empty.
 * Add real members (with permitted headshots in `photo`, or leave it out for a monogram tile)
 * and the section appears automatically.
 */
export const team: TeamMember[] = [];
